/* global define, it, describe, beforeEach, afterEach, expect, assert, require */
require('./../../helpers/chai');
require('./../../helpers/db-utils');

describe('MongoDB setup', function() {
  it('connects to the database', function() {
    // TODO begin testing USER model here
    assert.isTrue(false);
  });
  it('creates a collection');
  it('creates a record in the collection');
});
